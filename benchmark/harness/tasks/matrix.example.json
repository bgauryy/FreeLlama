{
  "schema_version": 1,
  "judge_model": null,
  "models": [
    {
      "id": "qwen-local",
      "agent": "your-agent-cli",
      "agent_command": "your-agent --model {model} --prompt {prompt_file} --workspace {workspace} --result {result_file}",
      "capabilities": ["filesystem", "shell", "tools", "skills", "mcp"]
    },
    {
      "id": "gemma-local",
      "agent": "your-agent-cli",
      "agent_command": "your-agent --model {model} --prompt {prompt_file} --workspace {workspace} --result {result_file}",
      "capabilities": ["filesystem", "shell", "tools"]
    }
  ]
}
