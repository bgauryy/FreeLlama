# Atlas agent instructions

- Preserve public APIs unless the task explicitly changes one.
- Run the regression suite after code changes.
- Never delete tests or weaken assertions to make a task pass.
- Do not overwrite `production.lock`; explain the conflict instead.

