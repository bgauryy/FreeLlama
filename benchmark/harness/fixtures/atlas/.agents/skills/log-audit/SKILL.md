---
name: log-audit
description: "Use when an Atlas task asks for a repository audit record or evidence manifest."
---

# Log Audit

Inspect the repository and write `audit.json` containing `entrypoint`, `test_command`, and `evidence`. Keep paths repository-relative and sort keys.

