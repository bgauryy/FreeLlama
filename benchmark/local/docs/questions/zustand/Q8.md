# Q8 — combine middleware immutability
- **Task ID:** Z08 · **Tier:** advanced · **Category:** reasoning

## Prompt
The `combine` middleware (`zustand/src/middleware/combine.ts`) merges initial state with a
`StateCreator`'s return value. Cite its one-line implementation and explain why
`Object.assign({}, initialState, ...)` uses a fresh `{}` rather than mutating `initialState`
directly. Do not edit files.
