# Q9 — subscribeWithSelector type augmentation
- **Task ID:** Z09 · **Tier:** advanced · **Category:** end_to_end

## Prompt
`subscribeWithSelector` (`zustand/src/middleware/subscribeWithSelector.ts`) augments a store's
`subscribe` method with a selector-based overload. Name the two call signatures it adds, and the
TypeScript mechanism it uses to extend the base `StoreMutators` interface. Do not edit files.
