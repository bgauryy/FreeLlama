# Q10 — devtools stack trace parsing
- **Task ID:** Z10 · **Tier:** complex · **Category:** reasoning

## Prompt
Trace how `devtools` (`zustand/src/middleware/devtools.ts`) matches a Redux DevTools action back to
its caller. Name the function responsible, and the two regex patterns it uses to parse a stack
trace line (one per JS engine family). Do not edit files.
