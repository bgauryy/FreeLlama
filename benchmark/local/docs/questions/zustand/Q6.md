# Q6 — useShallow render caching
- **Task ID:** Z06 · **Tier:** core · **Category:** reasoning

## Prompt
How does `useShallow` (`zustand/src/react/shallow.ts`) avoid recomputing on every render when the
shallow-equal result hasn't changed? Name the React hook it uses to persist state across renders.
Do not edit files.
