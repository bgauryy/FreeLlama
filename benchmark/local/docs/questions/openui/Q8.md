# Q8 — Variable substitution in AST
- **Task ID:** O08 · **Tier:** advanced · **Category:** efficiency

## Prompt
Which module-private function in `runtime/evaluator.ts` substitutes a bound variable reference
inside an AST node (used, for example, when evaluating the lazy `Each` builtin over a loop
variable)? Cite its exact signature (parameter names and types). Do not edit files.
