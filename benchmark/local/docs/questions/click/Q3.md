# Q3 — @click.command() mechanics
- **Task ID:** L03 · **Tier:** core · **Category:** reasoning

## Prompt
Explain how the `@click.command()` decorator (in `click/src/click/decorators.py`) turns a plain
function into a `Command` instance. Name the exact attribute the decorator reads from the function
to collect `@option`/`@argument` decorations, and the default command class used when `cls` is not
given. Do not edit files.
