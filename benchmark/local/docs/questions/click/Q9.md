# Q9 — main() to callback call chain
- **Task ID:** L09 · **Tier:** advanced · **Category:** end_to_end

## Prompt
Trace click's execution chain for a `Group` with one subcommand invoked from the command line,
starting at `BaseCommand.main()`. Name, in order, every method call from `main()` down to the
subcommand's own callback executing — mention `make_context`, `Group.invoke`, `resolve_command`,
and `ctx.invoke`. All of this is in one file — cite it. Do not edit files.
