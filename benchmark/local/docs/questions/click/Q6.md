# Q6 — no_args_is_help handling
- **Task ID:** L06 · **Tier:** core · **Category:** reasoning

## Prompt
Trace what happens when a `Group` is invoked with no arguments and `no_args_is_help` is true. Cite
the file where this check occurs for `Group`, and explain why the same check also appears
separately for a plain `Command`, and how `Group` computes its own default for that flag. Do not
edit files.
