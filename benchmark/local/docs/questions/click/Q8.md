# Q8 — Custom text wrapper
- **Task ID:** L08 · **Tier:** advanced · **Category:** reasoning

## Prompt
click's help text wrapping (`wrap_text` in `click/src/click/formatting.py`) relies on a custom
subclass of Python's standard `textwrap.TextWrapper`. Name the file and exact class name of that
subclass, and state what changed about how width is measured as of version 8.4.0 per `wrap_text`'s
own versionchanged note. Do not edit files.
