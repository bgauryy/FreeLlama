# Build-time copies of benchmark/local/scripts adapters (source of truth lives there).
adapters/

# Generated by eval-small.mjs — measurements are machine-specific, not repo data.
eval-small-results.json
